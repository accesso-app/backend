name: AWS Lambda DEV CI

on:
  push:
    branches:
      - master

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Setup ZIG
        uses: nektro/actions-setup-zig@v1

      - name: Setup toolchain
        uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true

      - name: Reuse cache
        uses: Swatinem/rust-cache@v2
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Install cargo-lambda
        uses: zerj9/setup-cargo-lambda@v0.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build lambda function
        run: cargo lambda build --release --arm64 --package accesso-api-internal

      - name: Deploy lambda function
        run: cargo lambda deploy --iam-role ${{ secrets.IAM_ROLE }} accesso-api-internal
